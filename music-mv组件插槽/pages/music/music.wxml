<!-- index.wxml -->
<view class="container-grid">
  <v-item wx:for="{{musics}}" data="{{item}}">
    <view class="icon" slot="icon"><image src="/images/p0.png"></image>{{item.playCount}}</view>
  </v-item>
</view>